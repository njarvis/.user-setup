```bash
# Clone and install user setup
cd $HOME; git clone https://github.com/njarvis/.user-setup && ~/.user-setup/installer.sh

# Clone and install Emacs setup
cd $HOME; git clone https://github.com/njarvis/.emacs.d
```
