```bash
# Clone and install user setup (HTTP)
cd $HOME; git clone https://github.com/njarvis/.user-setup && ~/.user-setup/installer.sh

# -- or --

# Clone and install user setup (SSH)
cd $HOME; git clone git@github.com:njarvis/.user-setup.git && ~/.user-setup/installer.sh


# Clone and install Emacs setup (HTTP)
cd $HOME; git clone https://github.com/njarvis/.emacs.d

# -- or --

# Clone and install Emacs setup (GIT)
cd $HOME; git clone git@github.com:njarvis/.user-setup.git
```
